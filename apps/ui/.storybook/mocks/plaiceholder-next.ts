/**
 * Mock for `@plaiceholder/next`.
 * Identity function — passes the Next.js config through unchanged.
 */
export function withPlaiceholder(nextConfig: any) {
  return nextConfig
}

export default withPlaiceholder
